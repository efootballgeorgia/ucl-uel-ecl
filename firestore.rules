rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read the match results
    match /{leagueCollection}/{document=**} {
      allow read: if true;
      // Only allow authenticated users to write (create, update, delete)
      allow write: if true
    }
  }
}
